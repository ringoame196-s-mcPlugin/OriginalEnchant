package com.github.ringoame196_s_mcPlugin

import org.bukkit.plugin.Plugin

class EnchantRegister(plugin: Plugin) {
    /*
    val customEnchantMent = CustomEnchantment(NamespacedKey(plugin, "custom_enchant"), "Custom Enchant")


    fun register() {
        try {
            val f: Field = Enchantment::class.java.getDeclaredField("acceptingNew")
            f.isAccessible = true
            f.set(null, true)

            if (Enchantment.getByKey(customEnchantMent.key) == null) {
                Enchantment.registerEnchantment(customEnchantMent)
                Bukkit.getLogger().info("Custom Enchantment Registered!")
            }
        } catch (e: Exception) {
            e.printStackTrace()
        }
    }
     */
}
